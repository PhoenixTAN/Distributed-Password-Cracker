packet.json add  "homepage": ".",

npm run build

scp -i .\id_geni_ssh_rsa -r D:\Distributed-Password-Cracker\react-app\build\* ziqi1756@pcvm3-7.instageni.colorado.edu:/users/ziqi1756/reactapp

sudo cp -r ./* /var/www/html

sudo apt-get update
sudo apt install nginx-full

sudo chmod 777 nginx.conf

vim /etc/nginx/nginx.conf

http{
    # other code
    server {
        listen 80;
    
        location / { 
            root /var/www/html; 
            index index.html index.htm;
        }
    }
}

# cp your file to /var/www/html
sudo cp -r ./reactapp/* /var/www/html

# configuration file syntax
sudo nginx -t  

sudo service nginx start

sudo systemctl status nginx

